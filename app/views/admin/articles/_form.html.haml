- content_for :title, "#{controller.action_name.titleize} Article"
= render "shared/error_messages", :target => @article

.field
  = form.label :title
  = form.text_field :title
.field
  = form.label :body
  = form.text_area :body
.actions
  = form.submit 'Save'
